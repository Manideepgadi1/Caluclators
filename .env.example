# Production Environment Variables for Docker Compose
# Copy this file to .env in the root directory

# Domain Configuration
DOMAIN=yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# API URL (for frontend)
NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
ENVIRONMENT=production

# Security - CHANGE THIS!
# Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE-THIS-TO-A-SECURE-RANDOM-STRING-MINIMUM-32-CHARACTERS

# Logging
LOG_LEVEL=INFO

# Optional: Database connection (when you add database)
# DATABASE_URL=postgresql://user:password@db:5432/financialdb

# Optional: Redis (when you add caching)
# REDIS_URL=redis://redis:6379/0
